name: docs-preview-cleanup
on:
  pull_request_target:
    branches: [main, '[0-9]+.[0-9]+']
    types: [closed]
permissions:
  contents: none
  deployments: write
  id-token: write
jobs:
  cleanup:
    uses: elastic/docs-actions/.github/workflows/docs-preview-cleanup.yml@db1f8230419635afe51434fbd5b3c9214e2e5dc3 # v1

---
- name: file
  reusable:
    expected:
      - threat.indicator

  fields:
    - name: directory
      type: wildcard
    - name: path
      type: wildcard
    - name: target_path
      type: wildcard

---
- name: file
  reusable:
    expected:
      - threat.enrichments

  fields:
    - name: directory
      type: wildcard
    - name: path
      type: wildcard
    - name: target_path
      type: wildcard

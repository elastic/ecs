# Proposed reusable block for schemas/entity.yml.
# Field definitions are unchanged; only reuse order and expected locations change.

reusable:
  top_level: true
  order: 3
  expected:
    - user
    - user.target
    - host
    - service
    - at: entity
      as: target
      short_override: Targeted entity of action taken.
